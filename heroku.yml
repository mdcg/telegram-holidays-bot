build:
  docker:
    worker: Dockerfile
  config:
    DEBUG: 0
run:
  worker:
    command: >
      sh -c "python --version
      && python -m src.generate_db vacations.csv
      && sleep 5
      && python -m src.core"
    image: worker